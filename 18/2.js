import * as fs from 'fs';
import { Queue } from '@datastructures-js/priority-queue';

function solution() {
  let input = fs.readFileSync('./17/input.txt', { encoding: 'utf8', flag: 'r' }).split('\n');

  return 0;
}

console.log(solution());